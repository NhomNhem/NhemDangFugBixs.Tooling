# SceneComponents

Demonstrates registration patterns for Unity components:

- `[SceneComponent<TScope>]` for in-scene components.
- `RegisterInHierarchy` compatibility for legacy attributes.
- Constructor-injection guardrails (MonoBehaviour-safe injection style).

Use this sample to validate presentation-layer registration and analyzer behavior.
