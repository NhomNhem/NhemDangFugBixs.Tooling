using System.CodeDom.Compiler;
using System.Collections.Generic;
using System.Text;
using System.Linq;
using System.IO;
using NhemDangFugBixs.Common.Models;
using NhemDangFugBixs.Generators.Utils;

namespace NhemDangFugBixs.Generators.Emitters;

internal static class RegistrationEmitter {
    public static string GenerateSource(
        IEnumerable<ServiceInfo> services,
        IEnumerable<SceneInjectionInfo> sceneServices,
        string assemblyName,
        List<ScopeMappingInfo> scopeMappings = null,
        GenerationStats? stats = null) {

        // Improved sanitization: replace any non-alphanumeric with underscore but keep dots for stable hint names
        string sanitizedNamespace = new string(assemblyName.Select(c => char.IsLetterOrDigit(c) || c == '.' ? c : '_').ToArray()).Replace("..", ".");

        using var stringWriter = new StringWriter();
        using var writer = new IndentedTextWriter(stringWriter, "    ");

        // 1. Header
        writer.WriteLine("// <auto-generated />");
        if (stats != null) {
            writer.WriteLine($"// Generated by NhemDangFugBixs.Tooling {stats.Version}");
            writer.WriteLine($"// Date: {stats.Timestamp}");
            writer.WriteLine($"// Services: {stats.ServiceCount} registered");
            if (stats.Warnings.Count > 0) {
                writer.WriteLine("// Warnings:");
                foreach (var warning in stats.Warnings) {
                    writer.WriteLine($"// - {warning}");
                }
            }
        } else {
            writer.WriteLine("// Generated by NhemDangFugBixs.Tooling v4.0");
        }
        writer.WriteLine("// Robust indestructible C# emission");
        writer.WriteLine();
        writer.WriteLine("using System;");
        writer.WriteLine("using VContainer;");
        writer.WriteLine("using VContainer.Unity;");
        writer.WriteLine();

        using (writer.Block($"namespace NhemDangFugBixs.Generated.{sanitizedNamespace.Replace(".", "_")}")) {
            writer.WriteLine();

            // 1. Blueprint for Editor Discovery (Scene Injection)
            using (writer.Block("public static partial class SceneInjectionBlueprint")) {
                writer.WriteLine("public static readonly global::System.Type[] ComponentTypes = {");
                writer.Indent++;
                // Deduplicate scene services by full name
                foreach (var svc in sceneServices.GroupBy(s => s.FullName).Select(g => g.First())) {
                    writer.WriteLine($"typeof(global::{svc.FullName}),");
                }
                writer.Indent--;
                writer.WriteLine("};");
            }
            writer.WriteLine();

            // 2. Standard VContainer Registrations
            using (writer.Block("public static partial class VContainerRegistration")) {

                // MASTER ENTRY POINT
                if (scopeMappings != null && scopeMappings.Count > 0) {
                    using (writer.Block("public static void RegisterAll(global::VContainer.IContainerBuilder builder)")) {
                        foreach (var mapping in scopeMappings) {
                            string methodName = GetRegistrationMethodName(mapping.ClassName);
                            writer.WriteLine($"{methodName}(builder);");
                        }
                    }
                    writer.WriteLine();
                }

                // Deduplicate services by full name
                var distinctServices = services.GroupBy(s => $"{s.Namespace}.{s.ClassName}").Select(g => g.First());

                // Group services by their scope key
                var mappedServices = distinctServices.Select(s => {
                    if (s.UsesTypeSafeScope && scopeMappings != null) {
                        var mapping = scopeMappings.FirstOrDefault(m => m.IdentityTypeName == s.ScopeTypeName);
                        if (!string.IsNullOrEmpty(mapping.ClassName)) {
                            return MapToScope(s, mapping.ClassName);
                        }
                        
                        var directMapping = scopeMappings.FirstOrDefault(m => 
                            m.OriginalFullName == s.ScopeTypeName || 
                            m.OriginalClassName == s.ScopeTypeName ||
                            m.FullName == s.ScopeTypeName || 
                            m.ClassName == s.ScopeTypeName);
                            
                        if (!string.IsNullOrEmpty(directMapping.ClassName)) {
                            return MapToScope(s, directMapping.ClassName);
                        }
                    }
                    return s;
                });

                var groups = mappedServices.GroupBy(s => GetScopeKey(s)).ToDictionary(g => g.Key, g => g.ToList());

                foreach (var group in groups) {
                    string methodName = GetRegistrationMethodName(group.Key);

                    using (writer.Block($"public static void {methodName}(global::VContainer.IContainerBuilder builder)")) {
                        writer.WriteLine("global::VContainer.Unity.EntryPointsBuilder.EnsureDispatcherRegistered(builder);");
                        writer.WriteLine();

                        var installers = group.Value.Where(s => s.IsInstaller).OrderBy(s => s.InstallerOrder).ToList();
                        var exceptionHandlers = group.Value.Where(s => s.IsExceptionHandler).ToList();
                        var buildCallbacks = group.Value.Where(s => s.IsBuildCallback).ToList();
                        var standardServices = group.Value.Where(s => !s.IsInstaller && !s.IsExceptionHandler && !s.IsBuildCallback).ToList();
                        
                        // Mixed: Some ExceptionHandlers or BuildCallbacks might also be standard services (registered via builder.Register)
                        // but they are resolved and invoked in the Callbacks section.
                        var specialServices = group.Value.Where(s => s.IsExceptionHandler || s.IsBuildCallback).ToList();

                        // 1. Installers (Run first)
                        if (installers.Count > 0) {
                            writer.WriteLine("// Installers");
                            foreach (var installer in installers) {
                                writer.WriteLine($"new global::{installer.FullName}().Install(builder);");
                            }
                            writer.WriteLine();
                        }

                        // 2. Standard Service Registrations
                        if (standardServices.Count > 0 || specialServices.Count > 0) {
                            writer.WriteLine("// Service Registrations");
                            foreach (var svc in standardServices.Concat(specialServices)) {
                                EmitRegistration(writer, svc);
                            }
                            writer.WriteLine();
                        }

                        // 3. Special Callbacks (Run last)
                        if (exceptionHandlers.Count > 0 || buildCallbacks.Count > 0) {
                            writer.WriteLine("// Special Callbacks");
                            
                            foreach (var handler in exceptionHandlers) {
                                string handlerFullName = $"global::{handler.FullName}";
                                writer.WriteLine($"builder.RegisterEntryPointExceptionHandler(ex => {{");
                                writer.Indent++;
                                writer.WriteLine($"var handler = builder.Application.Resolve<{handlerFullName}>();");
                                writer.WriteLine($"handler.OnException(ex);");
                                writer.Indent--;
                                writer.WriteLine($"}});");
                            }

                            foreach (var callback in buildCallbacks) {
                                string callbackFullName = $"global::{callback.FullName}";
                                writer.WriteLine($"builder.RegisterBuildCallback(container => {{");
                                writer.Indent++;
                                writer.WriteLine($"var cb = container.Resolve<{callbackFullName}>();");
                                writer.WriteLine($"cb.OnBuild(container);");
                                writer.Indent--;
                                writer.WriteLine($"}});");
                            }
                        }
                    }
                    writer.WriteLine();
                }
            }
        }
        return stringWriter.ToString();
    }

    private static void EmitRegistration(IndentedTextWriter writer, ServiceInfo svc) {
        string fullName = $"global::{svc.FullName}";
        string lifetime = $"global::VContainer.Lifetime.{svc.Lifetime}";

        if (svc.RegisterInHierarchy) {
            string suffix = GetSmartSuffix(svc);
            writer.WriteLine($"builder.RegisterComponentInHierarchy<{fullName}>(){suffix};");
        } else if (svc.IsMessagePipeBroker) {
            string messageType = svc.MessageType != null ? $"global::{svc.MessageType}" : fullName;
            writer.WriteLine($"builder.RegisterMessageBroker<{messageType}>({lifetime});");
        } else if (svc.IsComponent) {
            string suffix = GetSmartSuffix(svc);
            writer.WriteLine($"builder.RegisterComponentOnNewGameObject<{fullName}>({lifetime}){suffix};");
        } else if (svc.IsFactory) {
            string suffix = GetSmartSuffix(svc);
            writer.WriteLine($"builder.RegisterFactory<{fullName}>(c => () => c.Resolve<{fullName}>(), {lifetime}){suffix};");
        } else if (svc.IsEntryPoint && !svc.IsExceptionHandler && !svc.IsBuildCallback) {
            // Smart EntryPoint Registration: VContainer handles lifecycle interfaces natively.
            // Only explicitly map to non-lifecycle interfaces.
            string suffix = GetSmartEntryPointSuffix(svc);
            writer.WriteLine($"builder.RegisterEntryPoint<{fullName}>({lifetime}){suffix};");
        } else {
            string suffix = GetSmartSuffix(svc);
            writer.WriteLine($"builder.Register<{fullName}>({lifetime}){suffix};");
        }
    }

    private static string GetSmartSuffix(ServiceInfo svc) {
        if (svc.AsTypes != null && svc.AsTypes.Length > 0) {
            return ".As(" + string.Join(", ", svc.AsTypes.Select(t => $"typeof(global::{t})")) + ")";
        }

        var suffix = "";
        bool shouldBindImplementedInterfaces = svc.AsImplementedInterfaces || (svc.IsComponent && svc.IsEntryPoint);
        if (shouldBindImplementedInterfaces) suffix += ".AsImplementedInterfaces()";
        if (svc.AsSelf) suffix += ".AsSelf()";
        return suffix;
    }

    private static string GetSmartEntryPointSuffix(ServiceInfo svc) {
        if (svc.AsTypes != null && svc.AsTypes.Length > 0) {
            return ".As(" + string.Join(", ", svc.AsTypes.Select(t => $"typeof(global::{t})")) + ")";
        }

        // Identify non-lifecycle interfaces
        var customInterfaces = svc.InterfaceNames
            .Where(i => !InterfaceUtils.IsVContainerEntryPoint(i))
            .ToList();

        if (customInterfaces.Count > 0) {
            // Map to custom interfaces explicitly
            var interfaces = string.Join(", ", customInterfaces.Select(i => $"typeof(global::{i})"));
            return $".As({interfaces}).AsSelf()";
        }

        // If it only implements lifecycle interfaces, RegisterEntryPoint handles it. 
        // We just add .AsSelf() to ensure it's resolvable by its own type.
        return ".AsSelf()";
    }

    private static ServiceInfo MapToScope(ServiceInfo s, string targetScopeName) {
        return new ServiceInfo(
            s.Namespace, s.ClassName, s.Lifetime, s.ScopeName,
            s.InterfaceNames, s.IsComponent, s.AsImplementedInterfaces,
            s.AsSelf, s.RegisterInHierarchy, s.AsTypes, s.IsEntryPoint,
            s.IsFactory, targetScopeName, true, s.IsExceptionHandler, s.IsBuildCallback,
            s.IsInstaller, s.InstallerOrder, s.IsMessagePipeBroker, s.MessageType, s.MessagePipeKind, s.Metadata);
    }

    private static string GetScopeKey(ServiceInfo service) {
        if (service.UsesTypeSafeScope && !string.IsNullOrEmpty(service.ScopeTypeName)) {
            return service.ScopeTypeName;
        }
        return service.ScopeName;
    }

    private static string GetRegistrationMethodName(string scopeKey) {
        string name = scopeKey;
        if (name.Contains(".")) {
            name = name.Split('.').Last();
        }

        if (name.EndsWith("LifetimeScope", StringComparison.Ordinal)) {
            name = name.Substring(0, name.Length - "LifetimeScope".Length);
            if (string.IsNullOrEmpty(name)) {
                return "RegisterGlobal";
            }
        }
        
        return $"Register{name}";
    }
}
